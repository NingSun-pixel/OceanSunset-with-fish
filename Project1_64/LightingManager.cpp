#include "LightingManager.h"
//
//// 获取光照方向
//glm::vec3 direction = LightingManager::getInstance().getLightDirection();
//std::cout << "Light Direction: " << direction.x << ", " << direction.y << ", " << direction.z << std::endl;
//
//// 修改光照方向
//LightingManager::getInstance().setLightDirection(glm::vec3(0.0f, -1.0f, 0.0f));
//
//// 获取光照颜色
//glm::vec3 color = LightingManager::getInstance().getLightColor();
//std::cout << "Light Color: " << color.r << ", " << color.g << ", " << color.b << std::endl;
//
//// 修改光照颜色
//LightingManager::getInstance().setLightColor(glm::vec3(0.8f, 0.8f, 0.8f));
//
//// 获取 smoothness
//double smoothness = LightingManager::getInstance().getSmoothness();
//std::cout << "Smoothness: " << smoothness << std::endl;
//
//// 修改 smoothness
//LightingManager::getInstance().setSmoothness(1.0);
